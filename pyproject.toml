[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "q-lock-attractor-engine"
version = "0.1.0"
description = "Q-LOCK Attractor Engine — identity-locked quantum circuit watermarking by AttraQtor Labs."
authors = [{name = "Nicholas Hensley"}]
requires-python = ">=3.9"
dependencies = ["numpy", "qiskit", "qiskit-aer"]

[project.optional-dependencies]
dev = ["pytest", "mypy"]
